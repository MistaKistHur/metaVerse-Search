/* ::>> create a class that can be used in the Ui,
        & in other scripts to join, split and splice
          Urls, Search terms & json data
          need to learn more <= still 
*/
export default class joinSplit{
  this.input   = input;
  this.inputs  = [];
  this.options = [];
}
// ::>>............
function splice(?){
spOutput =;
return output;
}
// ::>> ............................
function split(input, delimeter){
     if(input == url){
      //  https://stackoverflow.com/questions/3809401/what-is-a-good-regular-expression-to-match-a-url ::>> See if theres a shorter version...
      let expression = /(https?:\/\/(?:www\.|(?!www))[a-zA-Z0-9][a-zA-Z0-9-]+[a-zA-Z0-9]\.[^\s]{2,}|www\.[a-zA-Z0-9][a-zA-Z0-9-]+[a-zA-Z0-9]\.[^\s]{2,}|https?:\/\/(?:www\.|(?!www))[a-zA-Z0-9]+\.[^\s]{2,}|www\.[a-zA-Z0-9]+\.[^\s]{2,})/gi;
      let regex = new RegExp(expression);     
     }

return output;
}
// ::>> .................
function join(inputs, x){

return;
}

let searchTerm = input;
.splice();
.split();
.join();
